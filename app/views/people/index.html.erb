<h1>Listing people</h1>

<!--
#FIXME
Add code so that:
	1. generic users can only see their own profiles
	2. generic users only have show/edit option
	3. admin users can see all profiles
-->

<table>
  <tr>
    <th>Name</th>
    <th>Password</th>
    <th>Email</th>
    <th>Description</th>
    <th>Birthday</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @people.each do |person| %>
  <tr>
    <td><%= person.name %></td>
    <td><%= person.secret %></td>
    <td><%= person.email %></td>
    <td><%= person.description %></td>
    <td><%= person.birthday %></td>
    <td><%= link_to 'Show', person %></td>
    <td><%= link_to 'Edit', edit_person_path(person) %></td>
    <td><%= link_to 'Destroy', person, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Person', new_person_path %>
